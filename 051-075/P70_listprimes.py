from prime import list_primes
import pickle

# primes = list_primes(10**7)
# pickle.dump(primes, open('primes10million.p', 'wb'))

# primes = list_primes(10**6)
# pickle.dump(primes, open('primes1million.p', 'wb'))
